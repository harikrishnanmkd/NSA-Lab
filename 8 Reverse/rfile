exit 0 
cat rfile
done
mv temp file
head -n $line  file > temp 
line=`expr $line - 1`
tail -n 1 file  >> rfile
do
while [ $line -ge 1 ]
rm rfile
line=`cat file | wc -l`
cp $1 file
fi
exit 1
echo "syntax is <filename>"
then
if [ $# -ne 1 ]
#set -xv
# to reverse a file
